import { NbgdSortableHeaderDirective } from './nbgd-sortable-header.directive';

describe('NbgdSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NbgdSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
